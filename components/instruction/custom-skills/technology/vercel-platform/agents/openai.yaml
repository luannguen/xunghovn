interface:
  display_name: "Vercel Platform"
  short_description: "Guide safe Vercel deployment configuration"
  default_prompt: "Use $vercel-platform with the active project binding and verified repository evidence."
policy:
  allow_implicit_invocation: false
