# Project Custom Skills

This is Layer B for the current repository. It contains detector output, active bindings, and evidence-backed overlays. It is intentionally small.

Current profile: `template/uninstantiated`. Only `github-ci` has HIGH-confidence evidence and an active binding. No application technology or domain is inferred. Re-run detection after cloning/instantiation or any dependency, platform, schema, contract, or domain-model change.

Never add secrets, production data, personal data, or copied reusable rules here. Every project-specific assertion must cite repository evidence and an applicable scope.
